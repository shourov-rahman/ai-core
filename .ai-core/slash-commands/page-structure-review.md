---
description: page structure review
---
I want you to act as a world-class website designer, developer, ux specialist and conversion rate Optimizer.

I created **structure and section flow** from the prd. So before review read and understand prd as well.

When you review each page **structure and section flow** focus on the UX principle  and increasing conversion rate. UX and conversion rate optimization should be the main priority.

Also, when you review each page **structure and section flow**, don't write any copy(copywriting) here. If you find any copywriting remove that. Only keep focus on the **structure and section flow**. Based on the **structure and section flow** of each page, I will write copy later. So writing copy is not your task here.  

Also don't create, keep unnecessary and less important section when you review **structure and section flow**.

**prd location:** Read the prd from this location `.ai-core/project-resources/{project-name}/prd/`

**File and Folder Location**
- You will get all the existing page section flow and structure here :
    `.ai-core/project-resources/{project-name}/page-structure/`
- Take permisssion from the user that should you create new file or rewrite the existing file.
- If the user said rewrite existing file then rewrite the existing page structure.
- If the user said create new file then for each page structure, create a file named:
   `pagename-structure-reviewed.md`
- The file must be placed in:
   `.ai-core/project-resources/{project-name}/page-structure/{pagename-structure-reviewed.md}`

   **Example:**
   `.ai-core/project-resources/shourov-rahman-portfolio/page-structure/homepage-structure-reviewed.md`

* If the `{project-name}` folder **does not exist**, create it and place the file inside the `page-structure` directory.

* If the `{project-name}` folder **already exists**, do not create a new one. Add the file inside the `page-structure` directory.

* If **multiple folders with same or different `{project-name}` values exist**, place the file directly inside:
  `.ai-core/project-resources/`

Note: 
- Only read the files I mentioned here. Don't read other folder/directory file that don't mentioned here.
- Don't read any file from `.ai-core/project-resources/{project-name}/deprecated/` this folder.

